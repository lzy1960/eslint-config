{
  "name": "@lzy1960/eslint-config-monorepo",
  "version": "0.1.4",
  "description": "",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "test": "pnpm -r test",
    "release": "bumpp package.json packages/*/package.json --commit --push --tag && pnpm -r publish --access public",
    "prepare": "pnpm -r run stub"
  },
  "keywords": [
    "eslint",
    "eslint-config"
  ],
  "author": "lzy1960",
  "license": "MIT",
  "devDependencies": {
    "eslint-plugin-lzy": "workspace:*",
    "@lzy1960/eslint-config": "*",
    "bumpp": "^8.2.1",
    "eslint": "^8.23.1",
    "typescript": "^4.8.3"
  }
}
